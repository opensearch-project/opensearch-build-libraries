lib = library(identifier: "jenkins@main", retriever: legacySCM(scm))

pipeline {
    agent none
    stages {
        stage('uploadIndexFile') {
            steps {
                script {
                    uploadIndexFile(
                        indexFilePath: 'test'
                    )
                }
            }
        }
    }
}