lib = library(identifier: "jenkins@1.0.0", retriever: legacySCM(scm))

pipeline {
    agent none
    stages {
        stage('uploadIndexFile') {
            steps {
                script {
                    uploadIndexFile(
                        indexFilePath: 'test'
                    )
                }
            }
        }
    }
}